name: Shell Commands

on: [push]

jobs:
  run-sell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo "Hello world"
      - name: Multiline Script
        run: |
          node -v
          npm -v
